{
  "name": "generate-pr",
  "version": "1.0.0",
  "description": "Auto-generate PR template from git history",
  "bin": {
    "generate-pr": "generate-pr.sh"
  },
  "scripts": {
    "start": "./generate-pr.sh"
  }
}
