# Generate-PR (gen-pr)

Auto-generate Pull Request descriptions using Gemini.

## Setup

1. Ensure `gh` CLI is installed.
2. Create `.env` with `GEMINI_API_KEY=...`

## Usage

```bash
./generate-pr.sh [target-branch]
# example: ./generate-pr.sh develop
```
